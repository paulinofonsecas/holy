---
phase: 04-web-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/platform/platform_adapter.dart
  - lib/core/platform/web_persistence_adapter.dart
  - lib/core/platform/platform_adapter_test.dart
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - READ-01
  - OFF-01
user_setup: []

must_haves:
  truths:
    - "The app builds and runs on Flutter Web (HTML/CanvasKit) without runtime errors."
    - "App persistence works in browser (IndexedDB/sembast) and data survives reloads."
    - "Platform abstraction exists so web-specific implementations are selectable at runtime per D-01 (no conflicting decisions)."
  artifacts:
    - path: "lib/core/platform/platform_adapter.dart"
      provides: "Platform adapter contract and factory"
    - path: "lib/core/platform/web_persistence_adapter.dart"
      provides: "Web persistence implementation (IndexedDB/sembast)"
    - path: "pubspec.yaml"
      provides: "Dependencies updated (sembast + sembast_web)"
  key_links:
    - from: "lib/core/platform/platform_adapter.dart"
      to: "lib/core/services/storage_service.dart"
      via: "StorageService imports and uses PlatformAdapter.getPersistence()"
      pattern: "PlatformAdapter.getPersistence"

---

<objective>
Deliver Web platform abstraction and a working web persistence adapter so the app runs on Flutter Web and preserves offline data.

Purpose: Enable first-class web support and avoid duplicating platform checks across code by providing a single adapter contract.
Output: new adapter contract, web implementation, test verifying adapter selection, and pubspec updates.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create platform adapter contract + tests</name>
  <files>lib/core/platform/platform_adapter.dart, lib/core/platform/platform_adapter_test.dart</files>
  <action>
    - Add lib/core/platform/platform_adapter.dart exporting an abstract class PlatformAdapter with:
      - abstract PersistenceAdapter getPersistence();
      - static Future<PlatformAdapter> createForEnvironment();
    - Implement a small factory that chooses WebPlatformAdapter when kIsWeb and a default MobileAdapter otherwise (implementation stubs allowed here but exported types must be complete).
    - Add lib/core/platform/platform_adapter_test.dart with unit tests that assert the factory returns a PlatformAdapter and that getPersistence() returns a type implementing the expected interface. Use package:test or flutter_test consistent with repository patterns.
    - Reference: follow lib/core/design_system/ patterns for naming and exports per CLAUDE.md guidance.
  </action>
  <verify>
    <automated>flutter test --name "PlatformAdapter"</automated>
  </verify>
  <done>PlatformAdapter contract exists, tests added and passing locally (flutter test passes for the new test).</done>
</task>

<task type="auto">
  <name>Task 2: Implement Web persistence adapter (sembast/web)</name>
  <files>lib/core/platform/web_persistence_adapter.dart, pubspec.yaml</files>
  <action>
    - Update pubspec.yaml to add dependencies: sembast: ^4.0.0 (or compatible) and sembast_web: ^0.3.0 (pick versions consistent with project Dart SDK). Run flutter pub get as part of verification.
    - Create lib/core/platform/web_persistence_adapter.dart implementing PersistenceAdapter using sembast_web/IndexedDB for storage. API must match the PersistenceAdapter shape used by the app (open/close/get/put/delete). Do not change consumers; adapter should implement the same interface.
    - Wire PlatformAdapter.createForEnvironment() to return an instance that uses WebPersistenceAdapter when kIsWeb.
    - Add minimal error handling and graceful fallback to an in-memory store if IndexedDB is unavailable.
  </action>
  <verify>
    <automated>flutter analyze && flutter test --name "WebPersistence"</automated>
  </verify>
  <done>App compiles for web, and unit tests for WebPersistenceAdapter pass; pubspec.yaml updated and pub get succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Add CI job for Flutter Web build and runtime smoke check</name>
  <files>.github/workflows/web-build.yml</files>
  <action>
    - Add a GitHub Actions workflow .github/workflows/web-build.yml that runs on push to branches and PRs and executes: checkout, setup Flutter, flutter pub get, flutter analyze, flutter test (web-targeted tests), and flutter build web --no-tree-shake-icons as a smoke step.
    - The workflow should be conservative about secrets and avoid publishing artifacts. Keep steps idempotent and use official flutter/actions/setup-flutter or setup-sdk patterns.
  </action>
  <verify>
    <automated>git ls-files --error-unmatch .github/workflows/web-build.yml && (yamllint .github/workflows/web-build.yml || echo "yamllint not installed; file exists check passed")</automated>
  </verify>
  <done>CI workflow file present and lints/tests pass locally; push triggers CI (external verification) — CI configured in repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser (untrusted) -> App | Web assets (JS/WASM) and user input executed in browser context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| T-04-01 | S | web/wasm assets | mitigate | Use Content Security Policy, serve wasm from repo assets or verified CDN; validate signatures if possible. |
| T-04-02 | I | persistence adapter | mitigate | Validate and sanitize data read from IndexedDB before use; use typed models with safe parsing. |
| T-04-03 | R | CI workflow | mitigate | Restrict secrets in workflow, avoid exposing tokens; require PR review for workflow changes. |
</threat_model>

<verification>
Overall checks:
- flutter analyze returns 0 issues
- flutter test passes (new tests + existing suite)
- CI workflow file exists at .github/workflows/web-build.yml
- Lightweight local smoke: flutter build web completes without fatal errors (CI will confirm full build)
</verification>

<success_criteria>
- App compiles for web (flutter analyze + flutter test pass)
- Web persistence adapter implemented and verified by tests
- CI workflow added to exercise web build
</success_criteria>

<output>
After completion, create .planning/phases/04-web-migration/04-01-SUMMARY.md
</output>
